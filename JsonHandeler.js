var  jsFileWriter

var addNewMessage = function(token, teaminfoInput, users,time, message, days){
  var newMessage = {
    token: token,
    teamInfo: teaminfoInput,
    time: time,
    message: message,
    users: users
  }
}
